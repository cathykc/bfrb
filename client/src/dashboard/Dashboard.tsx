import * as React from 'react';

export default class Dashboard extends React.Component {
  public render(): JSX.Element {
    return(
      <div>
        This is page for viewing aggregated client data.
      </div>
    );
  }
}
